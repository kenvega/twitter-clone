<% if user_signed_in? %>
  <p>You are logged in</p>

  log out: <%= link_to "Log out", destroy_user_session_path, data: { "turbo-method": :delete }, class: "btn btn-primary" %>
<% else %>
  <h1>Hello world</h1>
  <h2>You need to log in or sign up</h2>

  <%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary btn-lg" %>
  <%= link_to "Log in", new_user_session_path, class: "btn btn-outline-primary btn-lg" %>

<% end %>