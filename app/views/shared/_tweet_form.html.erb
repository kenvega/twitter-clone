<div class="tweet hover d-flex py-3 px-3">
  <div class="flex-shrink-0">
    <% if current_user.avatar.present? %>
      <%= image_tag current_user.avatar, size: "48x48", class: "rounded-circle" %>
    <% else %>
      <%= image_tag "profile.svg", size: "48x48", class: "rounded-circle" %>
    <% end %>

  </div>

  <div class="flex-grow-1 ms-3">
    <%= form_with model: Tweet.new, url: form_submit_path, method: :post, class: "w-100", data: { controller: 'tweet-form' } do |f| %>
      <%= f.text_area :body , class: "w-100 form-control tweet-input", placeholder: "What's happening" %>

      <div class="d-flex justify-content-end">
        <%= f.submit button_label, class: "btn btn-lg btn-twitter rounded-pill" %>
      </div>
    <% end %>
  </div>
</div>
