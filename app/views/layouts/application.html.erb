<!DOCTYPE html>
<html>
  <head>
    <title>Twitter Clone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= favicon_link_tag asset_path("twitter.svg") %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <% if flash.any? %>
      <%= render "shared/flashes" %>
    <% end %>

    <% if user_signed_in? %>
      <% if controller_name == "usernames" %>
        <%= yield %>
      <% else %>
        <div class="container py-2">
          <div class="row">
            <%# always show sidebar when signed in no matter the page you are in %>
            <%= render 'shared/sidebar' %>

            <%# then show the rest of the page that is custom based on what you are seeing %>
            <%= yield %>
          </div>
        </div>
      <% end %>
    <% else %>
      <%= yield %>
    <% end %>

  </body>
</html>
